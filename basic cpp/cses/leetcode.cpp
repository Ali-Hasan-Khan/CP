#include<bits/stdc++.h>
using namespace std;


#define loop(x,n) for(int x = 0; x < n; ++x)
#define loop1S(x,n) for(int x = 1; x <= n; ++x)
#define revloop(x,n) for(int x = n-1; x>=0; --x)
#define ll long long

vector<vector<int>> intervals = {{3,4},{2,3},{1,2}};

bool checkRightInt(vector<int>& intv){	
	for(auto &i:intervals){
		if()
	}
}

void M436(){
	

	for(auto &i:intervals){
		cout << i[0] << " " << i[1] << endl;
	}
}

int main(){
	M436();
	return 0;
}